using UnityEngine;
using UnityEngine.UI;

public class Calories : MonoBehaviour
{
    /*public Text caloriesAmount;
    public int calories;
    public int clickCounter = 0;

    public void Update() 
    {
        if (clickCounter >= 3)
        {
            UpdateAmountText();
            clickCounter = 0;
        }
    }
    void UpdateAmountText(){
        calories += 3; // if the player clicks 3 times they get 3 calorie added and resets the counter to 0
        caloriesAmount.text = $"Calories Burned: {this.calories}"; // checks how many calories you have earned 
    }
    public void Click()
    {
        clickCounter++;   // adds 1 to the counter so the scripts know how much the player has pressed
    }*/
}
