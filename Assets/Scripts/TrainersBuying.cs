using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class TrainersBuying : MonoBehaviour
{
   /*GameObject HolderForTrainerCards;
   GameObject g;
   [SerializeField] private Transform ShopScrollView;

   void Start()
   {
       HolderForTrainerCards = ShopScrollView.GetChild(0).gameObject;

       for (int i = 0; i < 8; i++)
       {
           g = Instantiate(HolderForTrainerCards, ShopScrollView);
       }
       Destroy(HolderForTrainerCards);
       
   }   */ 
}
